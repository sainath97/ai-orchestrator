# Worked prompt

Goal: Prepare a PR for the completed change in `repos/<child-name>`.

Context: Read the parent and child project contexts and ticket `<ticket-id>`.

Constraints: Review only; do not commit, push, or open the PR. Use actual repository commands and report unavailable checks.

Acceptance criteria: PR body includes risk tier, changed files, verification, reviewers, and rollback.