# Worked prompt

Investigate incident `<incident-id>` affecting `repos/<child-name>`.

Use only evidence available from configured monitoring systems and the child repository. Build a timeline, list facts versus hypotheses, propose reversible mitigations, and report unavailable evidence. Do not execute production changes.