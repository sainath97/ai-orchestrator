# Worked prompt

Refactor only the duplicated parsing logic in `repos/<child-name>`.

First locate existing tests and add characterization coverage if the behavior is untested. Preserve inputs, outputs, errors, logging, and configuration. Do not modernize unrelated code. Run the narrowest available test command and report what was not run.