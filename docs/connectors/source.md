# Source connector

Status: TODO — no source host was provided.

When configured, use read-only access for repository discovery, branch metadata, and pull-request context. Child application PRs must target the child repository.

Manual fallback: open the child repository locally under `repos/` and use its Git CLI metadata.